RewriteEngine On

Options -Indexes

RewriteCond %{REQUEST_URI} ^/public/
RewriteRule ^ - [L]

# Route all non-file requests to index.php (directories also route to PHP so /admin doesn't conflict with the admin/ folder)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^(.*)$ index.php [QSA,L]
